#include <cmath>
#include <cstddef>
#include <cstdint>
#include <tuple>
enum message_pack_type{
    integer,
    nil_type,
    boolean,
    float_type,
    str,
    binary,
    array,
    map
};
